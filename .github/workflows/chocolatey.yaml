name: Publish Chocolatey Package

on:
  release:
    types: [published]

jobs:
  chocolatey:
    name: Publish to Chocolatey
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get release version
        id: version
        shell: bash
        run: echo "tag=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Download Windows binary and compute SHA256
        id: sha
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.tag }}"
          $url = "https://github.com/kexoz/portshare-cli/releases/download/v$version/portshare-windows-amd64.exe"
          Invoke-WebRequest -Uri $url -OutFile portshare.exe
          $sha = (Get-FileHash portshare.exe -Algorithm SHA256).Hash.ToLower()
          echo "sha=$sha" >> $env:GITHUB_OUTPUT

      - name: Update install script with real checksum
        shell: pwsh
        run: |
          $content = Get-Content packages/chocolatey/tools/chocolateyInstall.ps1 -Raw
          $content = $content -replace "`$version = '.*'", "`$version = '${{ steps.version.outputs.tag }}'"
          $content = $content -replace 'PLACEHOLDER_SHA256', "${{ steps.sha.outputs.sha }}"
          Set-Content packages/chocolatey/tools/chocolateyInstall.ps1 $content

      - name: Install Chocolatey and pack
        shell: pwsh
        run: |
          choco pack packages/chocolatey/portshare.nuspec --version ${{ steps.version.outputs.tag }} --output-directory packages/chocolatey/

      - name: Push to Chocolatey
        shell: pwsh
        run: |
          choco push packages/chocolatey/portshare.${{ steps.version.outputs.tag }}.nupkg --source https://push.chocolatey.org/ --api-key ${{ secrets.CHOCOLATEY_API_KEY }}
